# 串级PID控制原理说明

## 一、什么是串级PID？

串级PID就是**两个PID控制器串联起来工作**，一个管"去哪"（位置），一个管"怎么去"（速度）。

我们这个平衡球系统里：
- **外环（位置环）**：看球的位置偏了多少，决定球应该往哪滚、滚多快
- **内环（速度环）**：看球当前速度对不对，控制舵机倾斜来修正

## 二、和单环PID的区别

### 单环PID（之前的做法）

```
球的位置 ──→ PD计算 ──→ 舵机角度
```

一步到位，直接从位置算到舵机。

**问题**：就像开车只看"离目的地多远"，不管当前车速。球在飞速冲向中心时，控制器还在使劲推，结果冲过头又反向推，来回震荡。

### 串级PID（现在的做法）

```
球的位置 ──→ 位置环PD ──→ 目标速度 ──→ 速度环P ──→ 舵机角度
                              ↑
                        球的实际速度
```

分两步走：先算"球应该跑多快"，再算"舵机怎么倾斜才能让球跑到那个速度"。

## 三、详细工作流程

### 第1步：位置环（每20ms执行一次）

**输入**：球当前的位置误差

```
误差 = 球的位置 / 平台半径
```

比如球偏右5.7个像素，平台半径11.4像素，误差 = 0.5。

**计算**：位置环PD输出一个"目标速度"

```
目标速度 = pos_Kp × 误差 + pos_Kd × 误差变化率
```

- `pos_Kp × 误差`：球偏得越远，应该滚回来的速度越快
- `pos_Kd × 误差变化率`：球正在快速靠近中心时，提前减速，防止冲过头

**输出**：目标速度（比如 +2.0 表示球应该以速度2.0向中心滚）

### 第2步：速度环（每10ms执行一次）

**输入**：目标速度 vs 球的实际速度

```
速度误差 = 目标速度 - 实际速度
```

比如位置环说球应该以2.0的速度滚回来，但球实际速度只有0.5，速度误差 = 1.5，说明球跑太慢了。

**计算**：速度环P输出舵机修正量

```
舵机修正 = vel_Kp × 速度误差
```

球跑太慢→加大倾斜角度让球加速；球跑太快→减小倾斜角度让球减速。

**输出**：舵机角度

### 为什么速度环跑得更快？

速度环10ms执行一次，位置环20ms执行一次。

因为速度变化比位置变化快得多，需要更频繁地修正。就像开车时，你看路牌（位置）是几秒看一次，但你调油门刹车（速度）是随时在调的。

## 四、串级PID的优势

### 1. 各管各的，互不干扰

| 位置环管的事 | 速度环管的事 |
|-------------|-------------|
| 球要去哪 | 球怎么去 |
| 目标速度多大 | 舵机怎么调 |
| 接近中心时减速 | 速度不对时修正 |

调位置环不影响速度环的稳定性，反过来也一样。

### 2. 内环天然抗干扰

摄像头数据有噪声→速度计算有毛刺→但速度环每10ms就在修正，毛刺被内环消化掉了，不会直接影响位置环的判断。

### 3. 不容易过冲

位置环只输出一个"合理的速度目标"，速度环负责精确跟踪这个速度。球不会被猛推过头，因为速度环一直在"踩刹车"控制球速。

### 4. 调参更简单

单环PID：P和D互相牵扯，调一个另一个也变。

串级PID：先调速度环（让球能稳定地跟踪速度），再调位置环（让球精准停在中心）。两个环独立调，思路清晰。

## 五、代码对应关系

### 变量说明

| 变量 | 含义 |
|------|------|
| `pos_Kp` | 位置环比例系数：位置偏差多大→目标速度多大 |
| `pos_Kd` | 位置环微分系数：提前刹车的力度 |
| `vel_Kp` | 速度环比例系数：速度差多大→舵机修正多大 |
| `vel_Kd` | 速度环微分系数（目前未使用，预留） |
| `target_vx` | 位置环输出的X方向目标速度 |
| `target_vy` | 位置环输出的Y方向目标速度 |
| `pos_loop_cnt` | 位置环计数器，每2次速度环执行时触发位置环 |

### 代码结构

```c
// 每10ms执行一次（TIM4中断触发）
if (control_flag) {
    // ---- 速度环（每次必执行）----
    算当前速度
    算速度误差 = 目标速度 - 当前速度
    舵机角度 = 90° + vel_Kp × 速度误差
    输出PWM

    // ---- 位置环（每2次执行一次，即20ms）----
    pos_loop_cnt++;
    if (pos_loop_cnt >= 2) {
        算位置误差
        目标速度 = pos_Kp × 误差 + pos_Kd × 误差变化率
    }
}
```

## 六、调参指南

### 核心原则

**每次只调一个参数**，改一点看效果，别同时动好几个。

调参口诀：**先内后外，先P后D**。

### 调参顺序

#### 第1步：调速度环（内环）

先把位置环调大，让它给速度环"喂"一个明确的目标：

```c
pos_Kp = 5.0;   // 先调大，让位置环积极输出
pos_Kd = 0;     // 暂时不用
vel_Kp = 1.0;   // 从较小值开始
vel_Kd = 0;
```

然后观察串口输出：

| 现象 | 说明 | 调整 |
|------|------|------|
| 目标速度有输出，但舵机不动或动得很慢 | 速度环跟不上 | `vel_Kp` 从1.0慢慢加到3.0、5.0 |
| 舵机来回抖动，即使球不动 | 速度环响应过激 | `vel_Kp` 减小 |
| 舵机在球静止时有轻微抖动 | 速度死区太小 | 增大速度死区阈值（0.1→0.2） |

**速度环调好的标志**：球从一边滚向另一边时，舵机动作平滑跟随，不会突然猛推一下或慢半拍。

#### 第2步：调位置环（外环）

速度环调好后，回到位置环：

```c
pos_Kp = 1.5;   // 从小开始
pos_Kd = 0.5;
vel_Kp = 刚才调好的值;
```

| 现象 | 说明 | 调整 |
|------|------|------|
| 球偏了但平台不怎么倾斜，回不来 | 位置环推力不够 | `pos_Kp` 从1.5往上调 |
| 球冲过中心，来回跑好几趟 | 推太猛了 | 减小 `pos_Kp` 或增大 `pos_Kd` |
| 球快到中心时突然减速停住，但差一点点不动 | 刹车太早 | 减小 `pos_Kd` |
| 球在中心附近来回小幅震荡 | 微分项抖 | 减小 `pos_Kd` 或增大位置死区 |

**位置环调好的标志**：球放到任意位置，能平稳回到中心，不冲过头、不来回弹。

#### 第3步：微调

速度环调好之后基本不用再动了，后面主要调位置环的 `pos_Kp` 和 `pos_Kd` 就行。

### 速查表

| 现象 | 可能原因 | 调整方向 |
|------|----------|---------|
| 球不动 | 位置环输出太小 | 增大 `pos_Kp` |
| 球回中心很慢 | 推力不够 | 增大 `pos_Kp` |
| 球冲过头来回跑 | 刹车不够 | 增大 `pos_Kd` 或减小 `pos_Kp` |
| 球在中心附近抖 | 舵机过度修正 | 减小 `vel_Kp` |
| 球跟踪速度很迟钝 | 速度环响应慢 | 增大 `vel_Kp` |
| 球速度忽快忽慢 | 速度环不稳定 | 减小 `vel_Kp` |
